test('exemple de test', () => {
    expect(true).toBe(true);
  });
  

test('Exemple de test', () => {
// Arrange (Préparation) : Définissez les conditions initiales
    const a = 1;
    const b = 2;
  
    // Act (Action) : Exécutez le code à tester
    const result = a + b;
  
    // Assert (Assertion) : Vérifiez le résultat
    expect(result).toBe(3);
  });